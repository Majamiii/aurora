// globals.h
#ifndef GLOBALS_H
#define GLOBALS_H

#include <vector>

// Declare the global vector of random numbers
extern std::vector<float> randoms;

// Declare the function (if it’s not already defined inline)
float gen_random();

#endif // GLOBALS_H
